$(document).ready(function () {
  var Initialize = {
    init: function () {
      pageFade.init();
      toggleMenuClass.init();
      setPageHeight.init();
      toggleSidebar.init();
      instagram.init();
    }
  }

  Initialize.init();
});
