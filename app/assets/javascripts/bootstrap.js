$(document).ready(function () {
  var Initialize = {
    init: function () {
      // pageFade.init();
      toggleMenuClass.init();
      // setPageHeight.init();
      // instagram.init();
      scrollPos.init();
      smoothScroll.init();
      toggleMenu.init();
      heroCarousel.init();
    }
  }

  Initialize.init();
});
