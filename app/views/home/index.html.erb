<% page_meta(title: "Himesh Ladva | Portfolio") %>

<% content_for :stylesheets do %>
<% end %>

<% content_for :head do %>
<% end %>

<div class="home-section section">
  <%= render 'home' %>
</div>

<%= render 'shared/separator', last: false %>

<div class="about-section section">
  <%= render 'about' %>
</div>

<%= render 'shared/separator', last: false %>

<div class="portfolio-section section">
  <%= render 'portfolio' %>
</div>

<%= render 'shared/separator', last: false %>

<div class="contact-section section">
  <%= render 'contact' %>
</div>
